# Curipo 产品报告

> 文档状态：Living Document（持续更新）  
> 当前版本：v0.4  
> 更新日期：2026-06-18  
> 产品阶段：Alpha / 概念验证

---

## 1. 核心定位

Curipo 不是普通 AI 工具导航站，也不是游戏。

**Curipo V0 是一个拟人化 AI agent 名片夹：用户发现有可爱人格和明确专长的 agent，把它们像名片或球星卡一样存起来，再像推荐自己的经纪人一样分享给别人。**

用户不是在浏览一堆冷冰冰的工具，而是在认识一群有角色、有专长、有服务身份的 digital agents。

英文一句话：

> Collect AI agents worth remembering and referring.

中文一句话：

> Curipo 帮用户找到、记住、收藏并推荐值得认识的 AI agent。

品牌短句仍可使用：

> Find your AI crew.

## 2. 产品摘要

Curipo 的长期方向仍是 persona-based、relationship-based 的 AI agent 发现与推荐平台，但 V0 刻意缩小为一个轻量的 **personal agent card binder**。

用户发现一个有名字、有头像、有可爱人格、有明确服务专长的 agent，把它保存为一张数字卡片，并一键生成适合分享的页面或视觉卡片。分享的感觉不是转发软件链接，而是：

> “I know someone who can help — try Frank.”

产品由三个体验组合而成：

1. **发现**：通过搜索或精选卡片认识 agent。
2. **人格化卡片**：用名字、头像、个性、能力和服务身份帮助用户理解并记住 agent。
3. **个人名片夹**：把喜欢的 agent 像名片或球星卡一样保存起来。
4. **轻量 Refer**：一键生成适合 iMessage、Instagram 等渠道的分享页面或卡片。

卡片、头像、收藏和稀有度可以提供轻微游戏感，但它们只是表现形式。产品真正的逻辑是信任、熟悉、使用历史、复用和 refer。

## 3. 要解决的问题

### 用户问题

- AI 工具越来越多，但目录通常由陌生术语、同质化描述和 logo 组成。
- 用户知道自己想做什么，却不知道该搜索哪一类工具。
- 即使发现一个工具，也经常不知道该如何开始使用。
- 工具能力差异不透明，用户很难快速判断“它适不适合我”。
- 发现之后缺少一个轻量、个人化的方式保存和再次找到它们。
- 传统工具市场理性但无记忆点，浏览过程像筛选软件，而不是认识一个可靠的协作者。

典型流失路径：

> 我有需求 → 不知道该用哪个工具 / agent → 不知道怎么开始 → 懒得继续研究 → 放弃

Curipo 希望把它改写为：

> 我有需求 → Curipo 推荐几个 agent → 每个 agent 都像一个有专长的人 → 我选择、收藏并继续使用 → 用得好就推荐给朋友

### 核心洞察

人更容易理解并记住一个“擅长查资料、性格精确的侦探学者 Nova”，而不是又一个“AI research assistant”。更重要的是，人会对持续帮自己做事的对象产生熟悉、信任和偏好。

因此，Curipo 不只在做搜索结果的重新排版，而是在建立一种新的 AI 工具认知模型：

> 从“我该用哪个工具？”变成“这次任务，我该找谁？”

关键不是“可爱”，而是：

> 用角色感降低理解成本，用关系感增加复用和推荐。

## 4. 从游戏化到关系化

Curipo 最初的灵感包含头像、收藏、换装和游戏化 UI。进一步收束后，换装并不是核心需求，真正重要的是：

- 用户能记住 agent；
- 用户逐渐信任 agent；
- 用户会回来继续使用 agent；
- 用户愿意把 agent 推荐给朋友。

这种关系并不要求用户把 AI 当成宠物。现实里，人也会反复找同一个房产经纪、理财顾问、发型师、医生、律师、修车师傅或私人教练。选择不再来自每次重新比较，而来自“上次帮得不错”“懂我的偏好”“我信得过”。

Curipo 希望把这种服务关系带到 AI 时代：

> AI agents as digital service relationships.

游戏化与关系化的区别：

| 层次 | 具体表现 | 产品作用 |
|---|---|---|
| 游戏化表现 | 卡片、头像、收藏、稀有度 | 提升趣味、辨识度和记忆 |
| 关系化逻辑 | 信任、熟悉、使用历史、复用、refer | 形成长期价值与传播 |

## 5. 产品愿景

### V0 愿景

做一个让人愿意玩、愿意收藏、愿意转发的 AI agent 名片夹，并验证角色化 agent 是否比普通工具链接更容易被记住和推荐。

### 长期愿景

成为用户的个人 AI 协作层：每个人拥有一支了解自己偏好、可跨任务组合、能够真实执行工作的 AI crew。

长期护城河不应只是 agent 数量，而应来自：

- 对“任务与 agent 能力匹配”的理解；
- 用户长期形成的 crew 与偏好数据；
- 角色品牌、口碑与社区声誉；
- agent 在真实任务和公开挑战中的可验证表现。

## 6. 目标用户与使用情境

### 第一目标用户：AI 好奇者 / 轻度用户

知道 AI 能帮忙，但不熟悉模型和 agent 生态，希望直接用自然语言找到答案。

典型任务：订便宜机票、润色消息、做市场研究、解释理财概念。

### 第二目标用户：创作者与独立开发者

已经在使用多个 AI 工具，需要更快发现、比较和管理不同任务的助手。

典型任务：写内容、做设计、验证产品想法、生成或审查代码。

### 潜在供给侧用户：Agent 创作者

希望自己的 agent 被发现、被试用，并通过角色包装、任务表现和社区口碑建立品牌。

此角色不是首个 MVP 的主要服务对象，但会影响长期平台结构。

## 7. Jobs to Be Done

### 核心任务

> 当我有一件想交给 AI 做的事，但不知道该用什么时，我希望直接描述任务，并快速得到几个容易理解、值得尝试的选择。

### 辅助任务

- 当我看到一个不错的 agent 时，我希望保存它，之后不必重新寻找。
- 当多个 agent 看起来相似时，我希望知道推荐理由和适用边界。
- 当我再次面对类似任务时，我希望从自己的 crew 中直接选择。
- 当我准备真正使用某个 agent 时，我希望获得一个可以复制、修改的示例 prompt。
- 当一个 agent 确实帮到我时，我希望自然地把它推荐给有类似需求的朋友。

## 8. 产品原则

1. **任务优先，不让用户先学分类。** 搜索框首先问“你想做什么”。
2. **少而准。** 一次推荐 3–5 个候选，不制造新的选择焦虑。
3. **推荐必须可解释。** 每张卡都说明为什么匹配，而不只给分数。
4. **角色帮助理解，不能遮蔽能力。** 性格和视觉是入口，技能、适用场景与限制才是决策依据。
5. **关系感高于游戏感。** 卡牌是入口，复用与 refer 才是产品价值。
6. **先验证核心循环。** 在用户愿意“搜、看、收藏、回来”之前，不增加货币、战斗、装扮等系统。
7. **诚实描述 agent。** 明确它能做什么、不能做什么，以及是概念角色还是真实可用产品。
8. **当前版本统一使用英语。** 所有网页文案和数据库内容均使用英文，避免中英混杂；其他语言在后续本地化阶段统一加入。

## 9. 核心概念模型

### Agent

一个可被发现、理解和使用的 AI 能力单元。它同时包含：

- 身份：名字、头像、角色、性格；
- 能力：技能、使用场景、复杂度；
- 决策信息：最适合、不适合、价格、评分或验证记录；
- 使用入口：示例 prompt、外部链接或未来的直接执行能力；
- 收藏属性：稀有度、卡面与未来可选的成长记录。

### Mission

用户用自然语言描述的真实任务，是发现流程的起点。当前代码中使用 `mission brief` 作为搜索叙事，适合保留。

### Card Binder / My Crew

用户保存在当前浏览器中的个人 agent 卡册。V0 不把它设计成团队、社交主页或跨设备账户资产，只需要让收藏行为有满足感，并让用户之后容易再次找到和分享这些卡。

### Refer

用户把一个已经理解或认可的 agent 推荐给朋友。相比收藏，refer 更接近公开背书，也是 Curipo 区别于普通工具目录的重要传播行为。

V0 不建立好友关系或社交图谱，只提供公开 agent 页面、分享链接和可复制的推荐文案。

### Agent Card

每个 agent 的最小表达单元：名字、头像、人格、服务身份、专长、适用场景和分享视觉。它既是发现结果，也是可以收藏和 refer 的数字名片。

## 10. V0 核心体验闭环

1. 用户进入 Curipo，通过搜索或精选卡片发现 agent。
2. 用户看到一张有名字、头像、人格和明确专长的 agent 卡。
3. 用户打开详情，确认它是谁、能帮什么忙以及为什么值得记住。
4. 用户把 agent 加入自己的 Card Binder / My Crew。
5. 用户点击 Share，生成公开分享页面或适合社交渠道的视觉卡片。
6. 用户通过 iMessage、Instagram 或复制链接把 agent 推荐给别人。

MVP 最关键的验证闭环是：

> 发现角色 → 记住它 → 收进名片夹 → 生成分享卡 → Refer 给别人

V0 不要求接收者注册，也不追踪好友关系。分享页面打开即看，尽可能减少传播链路中的摩擦。

## 11. 信息架构

| 页面 | 路径 | 核心目标 | MVP 状态 |
|---|---|---|---|
| Discover / 首页 | `/` | 搜索或浏览精选 agent 卡片 | 已有 Alpha 页面，需调整叙事 |
| Results / 推荐结果 | `/results?q=...` | 比较 3–5 个匹配 agent | 已有 Alpha 页面 |
| Agent Card / 详情 | `/agent/[id]` | 理解角色并促成收藏或 refer | 已有 Alpha 页面 |
| Card Binder / My Crew | `/crew` | 查看与管理本地收藏的 agent 卡 | 尚未实现 |
| Share Page | `/agent/[id]` 或分享模式 | 为消息和社交平台提供公开预览 | 尚未实现 |

建议导航保持极简：`Curipo`、`Discover`、`My Crew`。

## 12. MVP 功能规格

### 12.1 Discover

- 清楚表达“发现并收藏值得认识的 AI agents”。
- 提供一个大号任务输入框。
- 支持 Enter 提交、Shift + Enter 换行，并兼容中文输入法。
- 提供 3–4 个 starter missions，帮助用户理解可输入内容。
- 显示精选 agent 卡，提前建立“数字名片 / 球星卡收藏”的预期。

### 12.2 Results

- 从 URL 参数 `q` 读取任务。
- 返回最多 5 个候选 agent。
- 每个候选显示名字、角色、头像、稀有度、简述和匹配理由。
- 用户可以在卡组中切换，并进入 agent 详情。
- 无匹配时展示友好 fallback，而不是空白页。

### 12.3 Agent Card Profile

- 展示身份、描述、能力、价格、评分、支持模型与外部链接。
- 中期补充：Personality、Skills、Use Cases、Best For、Not For、Sample Prompt。
- 主 CTA 为 `Add to My Crew` 或 `Save this card`。
- 次级 CTA 为 `Share agent`，生成带 agent 名字、人格与专长的自然推荐语。
- 外部产品链接可以保留为辅助信息，但 V0 不以真实调用转化为核心目标。

### 12.4 Card Binder / My Crew

- MVP 使用 `localStorage` 保存 agent ID，无需登录。
- 卡牌上直接显示 Added 状态。
- 页面以个人卡册形式展示已保存 agent，支持查看和移除。
- 空状态引导回 Discover。
- 不承诺跨设备同步；引入账户后再迁移到数据库。

### 12.5 Share Card / Lightweight Referral

- 每个 agent 都有可公开访问、无需登录的分享页面。
- 分享页面配置清晰的 Open Graph 图片和文案，使链接在 iMessage 中呈现为完整预览卡。
- 支持复制 agent 页面链接和一段带角色名字、人格与专长的推荐语。
- 支持生成适合 Instagram Story / Post 的竖版或方形视觉卡片；浏览器支持 Web Share API 时调用系统分享，否则保存图片或复制链接。
- Instagram 通常不能由网页无摩擦地直接发布内容，因此 V0 的可靠降级方案是生成可保存、可手动发布的图片。
- V0 不做好友、邀请、通知、权限、站内私信或 referral rewards。

## 13. 数据与匹配

### 当前技术架构（已确认）

- **前端与部署**：Next.js 部署于 Vercel。
- **后端与数据**：Supabase 负责 PostgreSQL 数据库，并可在需要时继续承载 Authentication、Storage 与 Row Level Security。
- **服务端逻辑**：轻量匹配和数据编排放在 Next.js Route Handlers 中，与前端一同部署于 Vercel。
- **当前判断**：这套架构足以支撑 MVP 和早期产品验证，不新增独立后端服务。

只有在出现明确瓶颈时才重新评估架构，例如长时间运行的异步任务、复杂队列、大规模 agent 调度，或已有数据证明的性能问题。当前优先级应是补齐产品闭环，而不是扩张基础设施。

### 理想 Agent 数据结构

```ts
type Agent = {
  id: string;
  name: string;
  role: string;
  tagline: string;
  personality: string;
  description: string;
  avatar?: string;
  rarity: "Common" | "Rare" | "Epic" | "Legendary";
  complexity: "Easy" | "Medium" | "Advanced";
  tags: string[];
  skills: string[];
  useCases: string[];
  bestFor: string[];
  notFor: string[];
  samplePrompt: string;
  models?: string[];
  price?: string;
  rating?: number;
  url?: string;
};
```

### MVP 匹配策略

当前使用关键词匹配是合理的 Alpha 方案：将查询小写并分词，对名字、tagline、模型和 description 进行加权命中，再按分数与评分排序。

下一步应让匹配覆盖 `role`、`tags`、`skills` 和 `useCases`，并输出具体命中依据。例如：

> Good match because Nova specializes in source comparison and market research.

不要在核心流程被验证前急于接入昂贵的语义搜索或 AI API。关键词方案的目标不是做到完美推荐，而是验证用户是否喜欢这种发现方式。

## 14. 角色阵容方向

首批内容应覆盖高频且差异清楚的任务：

| Agent | 角色 | 主要任务 | 个性方向 |
|---|---|---|---|
| Frank | Flight Agent | 航班搜索、价格追踪、路线比较 | 老练、冷静、轻微讽刺 |
| Nova | Research Agent | 研究、信源比较、总结 | 好奇、精确、投入 |
| Pip | Writing Agent | 改写、语气、邮件、社媒 | 机灵、快速、高情商 |
| Atlas | Strategy Agent | 产品策略、优先级、路线图 | 结构化、理性、全局观 |
| Mina | Design Agent | UI 方向、品牌、设计反馈 | 有品位、直接、审美明确 |
| Ledger | Finance Agent | 财务概念、预算、风险比较 | 谨慎、分析型、冷幽默 |
| Echo | Social Agent | 回复、外联、社交语气 | 友好、会来事、稍戏剧化 |
| Byte | Coding Agent | 编码、调试、重构 | 急躁但聪明 |

这些角色目前是产品内容方向，不代表仓库数据库中已存在对应完整记录。

## 15. 当前实现快照（2026-06-18）

### 已实现

- Next.js 16、React 19、TypeScript、Tailwind CSS 4 项目骨架。
- 前端与 Next.js API 路由使用 Vercel 部署，Supabase 作为当前后端与数据库。
- 首页 mission 输入、示例任务、卡牌预览与跳转。
- Results 页面及卡组式浏览交互。
- Agent 详情页。
- `/api/match` 关键词加权匹配接口。
- Supabase agent 表、公共读取策略与示例数据。
- 当前视觉形成了明显的“draft room / playable cards / roster”语言。

### 与最初 brief 不同但值得保留的演化

- 数据来源从“本地 JSON”演化为 Supabase。
- 视觉从“紫色、柔和、圆润”演化为更硬朗的编辑风卡牌：深色描边、琥珀色投影、纸张感。
- 产品叙事从“收藏角色”进一步走向“招募一支 agent 队伍”。
- 当前详情页开始引入公开挑战、声誉和 proof of work 的长期想象。

这些变化提升了辨识度，但需要通过用户测试确认它们是否同时保留了易用与可信。

### 仍未闭环

- `/crew` 页面与本地收藏逻辑尚未实现。
- 公开详情页已经具备分享 URL 基础，但尚未实现 Share / Copy recommendation 交互。
- 当前 Agent 数据模型偏工具目录，缺少 role、personality、skills、useCases、bestFor、notFor 和 samplePrompt。
- 数据库示例仍是 InboxZero、Soar、ReviewBot，尚未落地八个完整角色。
- 匹配理由目前较通用，尚未稳定对应具体技能。
- README 仍是 create-next-app 默认内容，尚未记录产品和运行配置。
- 依赖中尚未包含 Framer Motion；当前动效主要由 CSS 或页面交互承担。
- 未见统一 Navbar、AgentCard、RarityBadge 等可复用组件层。
- 没有产品分析事件，无法判断发现与收藏闭环是否成立。

## 16. MVP 边界

### 本阶段要做

- 8–20 个高质量、人格鲜明的 agent 卡片；
- 任务输入与可解释匹配；
- 卡牌比较和详情页；
- 本地 Card Binder / My Crew；
- 公开 agent 分享页、Open Graph 预览、分享图片与复制推荐语；
- 基础行为分析和 5–10 位目标用户测试。

### 明确不做

- 游戏引擎、地图、房间、战斗；
- 换装、金币、复杂经验值系统；
- 实时聊天；
- 好友系统、邀请、通知、权限和社交图谱；
- 用户账户与登录；
- UGC、agent 创建器和用户投稿；
- 支付；
- 真实 agent 调用；
- 大型创作者后台；
- 为了“看起来智能”而过早接入 AI 推荐；
- 在核心发现体验未验证前构建完整社交网络。

## 17. 核心假设与验证

| 假设 | 如何验证 | 成功信号 |
|---|---|---|
| 用户愿意用自然语言描述任务，而非按分类浏览 | 首页可用性测试、搜索提交率 | 新访客能不经解释完成首次搜索 |
| 角色化能提升理解和记忆 | 对比普通工具卡与角色卡 | 用户能复述 agent 名字与能力 |
| 3–5 个结果比长目录更容易决策 | 观察点击时间与犹豫点 | 大多数用户能在 1 分钟内选出候选 |
| 匹配理由能建立信任 | 访谈“为什么点它” | 用户引用能力或理由，而非只看卡面 |
| 数字卡册具有收藏吸引力 | 保存行为与访谈 | 用户愿意保存至少一张 agent 卡，而不只是浏览 |
| 卡牌/RPG 语言不损害专业可信度 | 定性访谈 | 用户觉得有趣但仍愿用于真实工作 |
| 用户愿意用自己的声誉为 agent 背书 | Share / Copy recommendation 行为与访谈 | 用户在没有奖励的情况下主动分享 agent |
| 分享卡比普通工具链接更有传播力 | 分享完成率与接收者点击 | 用户愿意把视觉卡片发到消息或社交平台 |

## 18. 建议指标

### 北极星候选

V0 的北极星候选为 **每周完成的有效 Agent Referral 数**。

有效 referral 可暂时定义为用户触发系统分享、复制分享链接或生成分享图片。条件允许时，再进一步统计分享页被另一设备打开的次数。

收藏是兴趣信号，分享更接近主动背书。V0 需要同时观察收藏率与分享率，但不以注册数、好友数或 UGC 数量作为成功指标。

### 漏斗指标

- 首页访问 → 提交 mission 的比例；
- 搜索提交 → 查看 agent 详情的比例；
- 详情查看 → 外部使用点击的比例；
- 搜索 / 详情 → 加入 Crew 的比例；
- 7 日内回访 Crew 的比例；
- Agent share / referral rate；
- 分享图片生成完成率；
- 分享页面被打开的次数；
- 收藏后再次打开 agent 的比例；
- 无结果率与重新搜索率；
- 从输入到首次有效选择的时间。

### 质量指标

- “推荐中至少有一个适合我”的主观评分；
- 用户是否能解释 agent 的差异；
- 一段时间后用户是否还记得 agent 名字；
- 错误或夸大能力的反馈率；
- 外部 agent 链接失效率。

## 19. 风险与应对

### 角色包装压过真实能力

风险：用户被卡面吸引，却无法判断 agent 是否真正可用。  
应对：详情页优先展示能力、限制、价格、验证记录和真实入口。

### 推荐质量不足

风险：少量数据与关键词匹配可能产生表面相关结果。  
应对：先缩小任务领域、手工提高内容质量、记录无结果 query，再决定是否引入语义匹配。

### “Agent”定义混乱

风险：目录中可能混合 prompt 角色、独立工具、自动化服务和真实自主 agent。  
应对：定义 agent 类型并明确标识，例如 `Character`、`Tool`、`Autonomous`、`Workflow`。

### 游戏化显得不可信或幼稚

风险：收藏和稀有度可能让专业用户误解产品。  
应对：稀有度不代表能力优劣；用“发现难度、专长独特性或策展等级”给出清楚定义，必要时从决策主区域降级。

### 冷启动

风险：agent 数量太少没有覆盖度，太多又降低内容质量。  
应对：从 8 个差异极强的角色开始，用真实搜索 query 决定扩充顺序。

## 20. 路线图

### Phase 0 — 产品基础（当前）

- 将产品叙事统一为 personal agent card binder；
- 完成 8 个核心角色内容；
- 补齐本地 Card Binder / My Crew；
- 补齐 Share Agent、Open Graph 分享页、分享图片与 Copy recommendation；
- 让详情页说明“能做什么、不能做什么、如何开始”；
- 更新 README 与环境配置说明。

### Phase 1 — 验证发现闭环

- 加入基本分析事件；
- 收集真实 mission 与无结果 query；
- 进行 5–10 位用户测试；
- 改进匹配权重和理由；
- 验证硬朗卡牌视觉与专业可信度的平衡。

### Phase 2 — 仅在 V0 验证后扩展

- 可选登录与云端 Card Binder；
- Public Binder 页面与基础 referral tracking；
- 增加使用后的反馈与评分；
- 引入 AI 语义匹配；
- 根据行为提供个性化推荐；
- Crew 跨设备同步。

### Phase 3 — Agent 生态

- 公共 agent profiles；
- 创作者提交与维护 agent；
- 公开任务挑战、表现记录和 reputation；
- agent skins、rooms、usage-based leveling 等轻量身份系统；
- 多 agent 组队与任务编排。
- 真实 agent 调用、API / MCP 接入、支付、审核和创作者分成。

## 21. 下一步优先级

### P0：让核心循环完整

1. 把首页和卡片叙事调整为 agent 名片夹，而不是招聘或游戏。
2. 实现 Card Binder 的本地保存、移除、状态与 `/crew` 页面。
3. 在 agent 详情加入 Share Agent、Copy link 与 Copy recommendation。
4. 为每个 agent 配置可分享的 Open Graph 卡面，并实现社交图片生成/保存。
5. 扩充 Agent schema，并在数据库或种子数据中落地 8 个角色。
6. 调整匹配逻辑，让推荐理由来自明确的 skill / use case 命中。
7. 明确每个 agent 是工具、外部服务还是概念角色，避免暗示 V0 可以直接调用。

### P1：让产品可验证

1. 记录 `agent_viewed`、`agent_saved`、`agent_reopened`、`share_started`、`share_link_copied`、`share_image_generated` 和 `shared_page_opened`。
2. 准备 10 个典型任务做匹配回归测试。
3. 邀请目标用户完成一次“找到一张喜欢的 agent 卡、收藏并分享”的任务测试。
4. 观察接收者能否从分享卡快速理解 agent，并愿意打开详情。

### P2：让项目可维护

1. 抽取 AgentCard、Navbar、RarityBadge、AgentAvatar 等组件。
2. 更新 README，补充 Supabase 环境变量与本地运行步骤。
3. 增加 loading、error、empty、无配置数据库等状态。

## 22. 待决策问题

这些问题不阻塞当前 MVP，但应随着验证逐项回答：

1. Curipo 的核心对象是“真实存在的第三方 agent”，还是“Curipo 自己创造并承载的角色 agent”？
2. V0 暂不提供真实 agent 调用；后续用户在 Curipo 内完成任务还是跳转到外部产品，需由验证结果决定。
3. `rarity` 到底表达什么？策展稀缺性、能力专业度，还是纯收藏属性？
4. Crew 是书签、工具箱，还是未来可以协同工作的多 agent 团队？
5. 平台第一垂直领域是什么：创作、工作效率、旅行，还是横向通用发现？
6. 长期的付费方是用户、agent 创作者，还是两者兼有？
7. 公开挑战与 reputation 是发现质量机制，还是独立的社区玩法？

## 23. 产品决策记录

| 日期 | 决策 | 原因 | 状态 |
|---|---|---|---|
| 2026-06-18 | 将 Curipo 定义为“角色化 agent 发现与收藏平台” | 统一搜索、角色和 Crew 三条叙事 | 已被 V0.4 进一步收束 |
| 2026-06-18 | 保留 roster / draft / mission 的招募语言 | 当前代码曾形成一致的招募表达 | 已被 card binder 叙事取代 |
| 2026-06-18 | MVP 不构建复杂游戏系统 | 集中验证发现、理解、收藏闭环 | 已确认 |
| 2026-06-18 | 当前 Alpha 继续使用关键词匹配 | 成本低、可解释、足以验证交互 | 暂定 |
| 2026-06-18 | Supabase 作为当前 agent 数据来源 | 当前代码已经接入，适合内容迭代 | 已实施 |
| 2026-06-18 | Vercel + Supabase 作为 MVP 与早期阶段技术架构 | 能覆盖部署、API、数据库及未来账户需求，当前无需独立后端 | 已确认 |
| 2026-06-18 | 将产品主轴从游戏化升级为关系化 | 角色感降低理解成本，信任、复用与 refer 才形成长期价值 | 已确认 |
| 2026-06-18 | Share / Refer 进入 V0 核心闭环 | 分享比收藏更接近用户主动背书，也提供自然传播 | 已确认 |
| 2026-06-18 | V0 不做好友系统 | 先验证用户是否愿意推荐 agent，避免过早引入社交图谱复杂度 | 已确认 |
| 2026-06-18 | V0 不做真实 agent 调用 | 当前先验证发现、记忆、收藏与推荐行为 | 已确认 |
| 2026-06-18 | 当前产品与数据库统一使用英语 | 保持内容体验一致，多语言留到后续本地化阶段 | 已确认 |
| 2026-06-18 | V0 收束为 personal agent card binder | 用最轻产品验证用户是否愿意收藏和分享人格化 agent | 已确认 |
| 2026-06-18 | V0 不做用户账户、好友或 UGC | 这些系统不属于当前核心假设，会显著增加产品重量 | 已确认 |
| 2026-06-18 | 社交分享优先适配 iMessage 与 Instagram | iMessage 使用 OG 链接预览；Instagram 使用可保存的分享图片作为可靠路径 | 已确认 |

---

## 附录 A：品牌语言参考

推荐保留的词：

- Mission：用户想完成的任务
- Agent：一个具有身份和专长的 AI 能力单元
- Roster：本次任务的候选阵容
- Crew：用户保存的长期队伍
- Dossier：agent 的详细档案
- Recruit / Add to Crew：收藏
- Refer / Recommend：把认可的 agent 推荐给别人

应谨慎使用的词：

- Playable：如果 agent 无法直接执行任务，可能造成错误预期。
- Rare / Epic / Legendary：必须定义依据，避免被理解为能力评级。
- Hire：如果没有付费或实际雇佣流程，应保持为品牌比喻。

## 附录 B：首轮用户测试脚本

给用户一个开放任务：

> 请在 Curipo 找到一张你喜欢或觉得有用的 agent 卡，把它存进自己的卡册，并把它推荐给一个可能需要它的朋友。

观察而不提示：

- 用户是否理解输入框要写什么；
- 是否注意并理解匹配理由；
- 如何在卡牌之间比较；
- 是否知道点击哪里看详情；
- 是否能找到并理解 Card Binder / My Crew；
- 是否能自然完成分享，而不需要注册或学习新概念；
- iMessage 链接预览或 Instagram 分享图是否能独立说明“它是谁、能帮什么”；
- 最后询问：“你为什么收藏它？你会真的把它发给谁？为什么？”
